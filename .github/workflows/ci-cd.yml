name: EVmarket CI/CD

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci # Dùng npm ci cho CI/CD để đảm bảo tính nhất quán

      - name: Run linter
        run: npm run lint

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: Build project
        run: npm run build

  deploy-staging:
    name: Deploy to Staging
    needs: build-and-test # Chạy sau khi build-and-test thành công
    if: github.ref == 'refs/heads/develop' && github.event_name == 'push' # Đảm bảo chỉ chạy khi push lên develop
    runs-on: ubuntu-latest
    environment: staging # Chỉ định môi trường để lấy secrets
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run database migrations
        run: npx prisma migrate deploy
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }} # Lấy từ secret của môi trường 'staging'

      - name: Trigger Render Deploy
        uses: fjogeleit/http-request-action@v1
        with:
          url: https://api.render.com/v1/services/${{ secrets.RENDER_SERVICE_ID }}/deploys
          method: POST
          customHeaders: |
            Authorization: Bearer ${{ secrets.RENDER_API_KEY }}
            Content-Type: application/json